from strategy import strategy

patient = strategy(instruments)
def fun(self, row_num, data, capital):
	'''A stock strategy:
	1, only consider stocks which have been stayed in bottom area for a long time
	2, entry when the prices have an abnormal up'''
	foreach i in self.instruments:
		if 	

patient.calculate = fun.__get__(patient, fun)
patient.run()

